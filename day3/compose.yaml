services:
  agent-api:
    build: .
    ports:
      - "8000:8000"
    env_file: .env
    environment:
      - MCP_URL=http://mcp:8001/mcp
    depends_on:
      - mcp

  mcp:
    build: .
    command: uv run --no-dev python src/mcp_server.py
    ports:
      - "8001:8001"